spring:
  datasource:
    url: jdbc:mysql://localhost:3306/coffee_shop?serverTimezone=Asia/Seoul
    username: test
    password: 123

  data:
    redis:
      host: localhost
      port: 6379

  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.MySQL8Dialect

  kafka:
    bootstrap-servers: localhost:9092

jwt:
  secret:
    key: test-secret-key-test-secret-key-test
  expiration: 1800000