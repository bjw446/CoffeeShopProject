spring:
  datasource:
    url: jdbc:mysql://localhost:3306/coffee-shop
    username: root
    password: root

  data:
    redis:
      host: localhost
      port: 6379

  jpa:
    hibernate:
      ddl-auto: create-drop

  kafka:
    bootstrap-servers: localhost:9092